<?xml version="1.0"?>
<ruleset name="ifthenpay Moodle Plugin">
  <description>PHPCS rules for the ifthenpay Moodle plugin.</description>

  <!-- Moodle standard (brings PHPCompatibility via Composer installer) -->
  <rule ref="moodle"/>

  <arg name="extensions" value="php"/>
  <arg name="basepath" value="."/>
  <arg name="parallel" value="8"/>

  <exclude-pattern>*/vendor/*</exclude-pattern>
  <exclude-pattern>*/tests/*</exclude-pattern>
</ruleset>
